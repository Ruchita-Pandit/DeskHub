{
  "name": "deskhub-jstraining",
  "version": "1.0.0",
  "type": "module",

  "scripts": {

    "dev": "vite",

    "server": "json-server --watch db.json --port 3001"
  },

  "dependencies": {},

  "devDependencies": {

    "json-server": "^1.0.0-beta.3",

    "vite": "^5.0.0"
  }
}